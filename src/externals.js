/**
 * parse-unit is a javascript library on npm for parsing
 * unit value strings into arrays.
 *
 * It serves some similar needs to {@link UnitValue} but is less featured.
 *
 * @external parse-unit
 * @see https://www.npmjs.com/package/parse-unit
 */
